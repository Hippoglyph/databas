--Give the total revenue generated by employees per city. List from greatest revenue to least revenue. List city names with the total per city. Note selling 100 units mean that revenue is unitPrice * 100.

SELECT employees.city, SUM(orderDetails.unitprice*orderDetails.quantity) as revenue
FROM orders, orderDetails, employees
WHERE orders.orderid = orderDetails.orderid AND employees.employeeid = orders.employeeid
GROUP BY employees.city
ORDER BY revenue DESC;